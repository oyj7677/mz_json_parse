{
  "name": "json-parse-format-download-tool",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy:vercel": "npx vercel --prod",
    "dev:vercel": "npx vercel dev",
    "start": "node server.js",
    "test": "node --test",
    "prepare:vendor": "node scripts/copy-xlsx-vendor.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "xlsx": "0.18.5"
  }
}
